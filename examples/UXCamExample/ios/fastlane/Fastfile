fastlane_version "2.47.0"
default_platform :ios

# Variables
scheme = "UXCamExample"
project = "#{scheme}.xcodeproj"
workspace = "#{scheme}.xcworkspace"

platform :ios do
  desc "Tests the app"
  lane :test do
    cocoapods
    scan(
      workspace: workspace,
      scheme: scheme,
      clean: true,
      devices: ["iPhone 6s"],
    )
  end
end
